import java.util.Scanner;
 public class inputs {
    public static void main(String[] args) {
        // Input
        Scanner sc = new Scanner(System.in);
        String name = sc.nextLine();    

        // sc.next = Prints only first word.
        // sc.nextLine = Print whole line.
        // sc.nextInt() = Prints integers values.
        // sc.nextFloat() = Prints decimal numbers.


        System.out.println(name);
    }
 }
